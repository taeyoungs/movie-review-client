import styled from '@emotion/styled';

const EA = styled.a`
    outline: none,
    border: none,
    color: inherit,
    text-decoration: none,
    cursor: pointer,
    width: 100%,
    height: auto,
    &:hover {
        text-decoration: none
    }
`;

export default EA;
