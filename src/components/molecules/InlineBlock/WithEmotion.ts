import styled from '@emotion/styled';

const EInlineBlock = styled.div`
  display: inline-block;
  width: 100%;
  height: auto;
  box-sizing: border-box;
  position: relative;
`;

export default EInlineBlock;
