import styled from '@emotion/styled';

const ELi = styled.li({
  width: 'auto',
  height: 'auto',
  display: 'block',
  margin: 0,
  boxSizing: 'border-box',
});

const EUl = styled.ul({
  width: 'auto',
  height: 'auto',
  boxSizing: 'border-box',
  display: 'flex',
  margin: 0,
  padding: 0,
});

export { ELi, EUl };
