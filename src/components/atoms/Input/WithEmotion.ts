import styled from '@emotion/styled';

const EInput = styled.input({
  outline: 'none',
  border: 'none',
  background: 'none',
  width: '100%',
  boxSizing: 'border-box',
});

export default EInput;
